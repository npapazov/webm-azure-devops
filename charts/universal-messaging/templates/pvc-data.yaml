apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "universal-messaging.fullname" . }}-data
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
